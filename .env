# Nodemailer SMTP configuration
# Replace the placeholders below with your actual SMTP credentials.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false  # true if using port 465 (SSL)
SMTP_USER=suneelsuther@gmail.com
SMTP_PASS=ywheojyuvmpthirf

# Email address that will receive the messages.
# If omitted, defaults to SMTP_USER
RECEIVER_EMAIL=suneelsuther@gmail.com

# Optional custom server port (defaults to 3001)
PORT=3001
